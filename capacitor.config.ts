import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.starter.angular',
  appName: 'cs-starter-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
